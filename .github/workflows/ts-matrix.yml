name: TS matrix

# Project-specific checks next to the shared moku-labs/ci pipeline (ci.yml): the TypeScript
# version matrix and the weekly run. publish.yml no longer calls this file.
on:
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - ".planning/**"
      - "specification/**"
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    # No paths-ignore here: `ci-pass` is a required check, and a required check that never
    # starts blocks a docs-only pull request for good.
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

jobs:
  lint:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft != true || github.event_name != 'pull_request'
    strategy:
      fail-fast: true
      matrix:
        ts-version: ["5.7", "5.8", "5.9", "6.0"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.8"
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun ci
      - if: matrix.ts-version != '6.0'
        run: bun add --no-save typescript@~${{ matrix.ts-version }}
      - run: bunx tsc --version
      - run: bun run lint

  typecheck:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft != true || github.event_name != 'pull_request'
    strategy:
      fail-fast: true
      matrix:
        ts-version: ["5.7", "5.8", "5.9", "6.0"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.8"
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun ci
      - if: matrix.ts-version != '6.0'
        run: bun add --no-save typescript@~${{ matrix.ts-version }}
      - run: bunx tsc --version
      - run: bunx tsc --noEmit

  test:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft != true || github.event_name != 'pull_request'
    strategy:
      fail-fast: true
      matrix:
        ts-version: ["5.7", "5.8", "5.9", "6.0"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.8"
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun ci
      - if: matrix.ts-version != '6.0'
        run: bun add --no-save typescript@~${{ matrix.ts-version }}
      - run: bunx tsc --version
      - run: bun run test:coverage
      - run: bun run test:sandbox

  build-validate:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft != true || github.event_name != 'pull_request'
    strategy:
      fail-fast: true
      matrix:
        ts-version: ["5.7", "5.8", "5.9", "6.0"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.8"
      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun ci
      - if: matrix.ts-version != '6.0'
        run: bun add --no-save typescript@~${{ matrix.ts-version }}
      - run: bunx tsc --version
      - run: bun run build
      - run: bun run validate

  ci-pass:
    if: always()
    needs: [lint, typecheck, test, build-validate]
    runs-on: ubuntu-latest
    steps:
      - name: Check job results
        run: |
          echo "lint: ${{ needs.lint.result }}"
          echo "typecheck: ${{ needs.typecheck.result }}"
          echo "test: ${{ needs.test.result }}"
          echo "build-validate: ${{ needs.build-validate.result }}"

          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            echo "::error::One or more jobs failed"
            exit 1
          fi

          if [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "::error::One or more jobs were cancelled"
            exit 1
          fi

          echo "All jobs passed successfully"
